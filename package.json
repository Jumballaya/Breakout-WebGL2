{
  "name": "breakout-gl",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview -- host"
  },
  "devDependencies": {
    "@types/gl-matrix": "^3.2.0",
    "typescript": "^5.2.2",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "gl-matrix": "^3.4.3"
  }
}
